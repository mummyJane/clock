# Alarm Module

This folder is the filesystem home for the built-in `alarm` module.

## Purpose

- store bedside alarm definitions
- schedule countdown, daily, and selected-day alarms
- play audio from the shared media library when an alarm triggers
- expose alarm stop handling to the bedside runtime and setup UI
