# Clock Module

This folder is the filesystem home for the built-in `clock` module.

## Purpose

- show the bedside clock surface
- provide module-specific settings in later tasks

## Status

Task 4 creates the module folder and registers the module in the setup web interface.
Task 5 will add the clock-specific settings and runtime behavior.
